branches: main

pipeline:
  publish:
    image: plugins/docker
    settings:
      dockerfile: Containerfile
      repo: docker-registry2.napuu.xyz/testings3
      tags: latest
      username: 
        from_secret: registry-user
      password:
        from_secret: registry-password
      tags:
      - latest
    when:
      event: push
